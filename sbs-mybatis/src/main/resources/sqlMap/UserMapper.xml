<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.miaoxinguo.sbs.dao.UserMapper">
  <resultMap id="BaseResultMap" type="io.github.miaoxinguo.sbs.entity.User">
    <!--
      @mbg.generated - 该元素由 Mybatis-Generator 自动创建，不要修改。
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="age" jdbcType="INTEGER" property="age" />
  </resultMap>
  <insert id="insert" parameterType="io.github.miaoxinguo.sbs.entity.User">
    <!--
      @mbg.generated - 该元素由 Mybatis-Generator 自动创建，不要修改。
    -->
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into user (name, age)
    values (#{name,jdbcType=VARCHAR}, #{age,jdbcType=INTEGER})
  </insert>
  <update id="updateByPrimaryKey" parameterType="io.github.miaoxinguo.sbs.entity.User">
    <!--
      @mbg.generated - 该元素由 Mybatis-Generator 自动创建，不要修改。
    -->
    update user
    set name = #{name,jdbcType=VARCHAR},
      age = #{age,jdbcType=INTEGER}
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    <!--
      @mbg.generated - 该元素由 Mybatis-Generator 自动创建，不要修改。
    -->
    select id, name, age
    from user
    where id = #{id,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    <!--
      @mbg.generated - 该元素由 Mybatis-Generator 自动创建，不要修改。
    -->
    select id, name, age
    from user
  </select>
  <select id="selectByQueryObject" resultMap="BaseResultMap">
    <!--
      @mbg.generated - 该元素由 Mybatis-Generator 自动创建，不要修改。
    -->
    select 
  </select>
</mapper>