<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>

    <context id="default" targetRuntime="io.github.miaoxinguo.sbs.IntrospectedTableCustomerImpl">
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>

        <plugin type="io.github.miaoxinguo.sbs.plugin.MapperPlugin"/>

        <!-- 设置注释：显示数据库的注释，定义生成时间格式 -->
        <commentGenerator type="io.github.miaoxinguo.sbs.comment.CustomerCommentGenerator">
            <property name="suppressDate" value="true"/>
            <property name="dateFormat" value="yyyy-MM-dd HH:mm"/>
        </commentGenerator>

        <jdbcConnection driverClass="com.mysql.jdbc.Driver"
                        connectionURL="jdbc:mysql://localhost:3306/sbs"
                        userId="sbs_root"
                        password="123456">
        </jdbcConnection>

        <!-- 类型转换 -->
        <javaTypeResolver type="io.github.miaoxinguo.sbs.resolver.JavaTypeResolverCustomerImpl">
            <!--
               true：数据库的数据类型 DECIMAL 和 NUMERIC 映射为 java.math.BigDecimal
               false：默认,
                   scale>0;length>18：使用BigDecimal;
                   scale=0;length[10,18]：使用Long；
                   scale=0;length[5,9]：使用Integer；
                   scale=0;length<5：使用Short；
            -->
            <property name="forceBigDecimals" value="true"/>
        </javaTypeResolver>

        <!-- 生成Model类存放位置 -->
        <javaModelGenerator targetPackage="tmp.entity" targetProject="sbs-mybatis-generator/src/main/java">
            <property name="enableSubPackages" value="true"/>

            <!-- 是否对类CHAR类型的列的数据进行trim操作 -->
            <property name="trimStrings" value="true"/>
        </javaModelGenerator>

        <!-- 生成映射文件存放位置 -->
        <sqlMapGenerator targetPackage="sqlMap" targetProject="sbs-mybatis/src/main/resources">
            <property name="enableSubPackages" value="true"/>
        </sqlMapGenerator>

        <!-- 不自动生成接口，写一个通用的泛型接口用于继承，解决生成覆盖的问题 -->
        <!-- 生成 Repository 类存放位置
        <javaClientGenerator type="XMLMAPPER" targetPackage="tmp.repository" targetProject="sbs-mybatis-generator/src/main/java">
            <property name="enableSubPackages" value="true"/>
            <property name="rootInterface" value="io.github.miaoxinguo.sbs.repository.PageableRepository"/>
        </javaClientGenerator>-->

        <!--生成对应表及类名-->
        <table tableName="user" enableDeleteByPrimaryKey="false">
            <!--<property name="rootInterface" value="io.github.miaoxinguo.sbs.modal.BaseRepository"/>-->
            <generatedKey column="id" sqlStatement="MySql" identity="true"/>
        </table>

        <table tableName="account" enableDeleteByPrimaryKey="false">
            <generatedKey column="id" sqlStatement="MySql" identity="true"/>
            <columnOverride column="user_id" javaType="User" property="user"/>
        </table>

    </context>

</generatorConfiguration>